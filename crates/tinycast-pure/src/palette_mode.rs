#[derive(Clone, Copy, Debug, Eq, PartialEq)]
pub enum PaletteMode {
    Launcher,
    Clipboard,
    CalculatorHistory,
    Emoji,
    FileSearch,
    Schedule,
    Uninstall,
    Quicklinks,
    QuicklinkArguments,
    Ai,
    AiHistory,
    ExtensionCommand,
}
